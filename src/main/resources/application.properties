spring.application.name=fruit_box_service
spring.profiles.active=local
# ===============================
# Database Configuration
# ===============================
spring.datasource.url=datasourcse
spring.datasource.username=postgres
#spring.datasource.password=password
spring.datasource.driver-class-name=driver

# ===============================
# JPA Configuration
# ===============================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# Flyway Configuration
# ===============================
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# ===============================
# Google Clint Configuration
# ===============================
#google.client-id=clint-id
#google.client-secret=secret-id
#google.redirect-uri=https://fruit-box-node-web.vercel.app/auth/callback

#spring.flyway.database.postgresql.supported-versions=16,17,18
